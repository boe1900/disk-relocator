# TODO（按优先级）

## P0（必须先完成）

- [x] T01 冻结画像与风险清单（unit 粒度）
  - 验收标准：画像 JSON 可被程序读取；每个画像包含 process/units；评审记录可追踪。

- [x] T02 定义核心数据模型与持久化 schema（relocations / operation_logs / health_snapshots）
  - 验收标准：可一键初始化本地数据库；可写入并查询完整迁移记录；重启后数据不丢失。

- [x] T03 固化 Tauri 命令契约与 DTO（scan_apps/get_disk_status/migrate_app/rollback_relocation/list_relocations/check_health）
  - 验收标准：命令签名与字段冻结；前后端类型对齐；空实现可编译并通过基础调用测试。

- [x] T04 实现预检引擎（进程运行态、权限、磁盘在线可写、空间余量、availability/unit 拦截）
  - 验收标准：每个预检项均可返回明确错误码与修复建议；单项失败可稳定拦截迁移。

- [x] T05 首次引导模式（首次直接落盘外接盘）
  - 验收标准：在“无历史数据”场景可直接创建目标目录并建立软链接；失败自动清理半成品；状态落盘为可恢复。

## P1（核心闭环）

- [x] T06 已有数据迁移事务（复制 -> 校验 -> 原子切换 -> 后检 -> 清理）
  - 验收标准：迁移成功后状态为 healthy；校验失败时不切换；切换失败自动回滚。

- [x] T07 回滚引擎（自动回滚 + 一键回滚）
  - 验收标准：任意失败阶段都可恢复原路径；回滚后应用可正常读写；元数据状态正确回退。

- [x] T08 中断恢复机制（崩溃/重启后恢复）
  - 验收标准：重启后可识别 unfinished 任务并恢复；不会出现“文件系统成功但元数据失败”的悬挂状态。

- [x] T09 迁移日志与 trace id
  - 验收标准：每次迁移有唯一 trace id；日志包含步骤、耗时、错误码；可导出。

## P2（稳定性与发布）

- [x] T10 健康监控（轮询 + 挂载事件）
  - 验收标准：断链、离线、只读可区分为 degraded/broken；状态刷新在约定周期内完成。

- [x] T11 健康面板与恢复引导
  - 验收标准：每个异常状态都有可执行指引；恢复后状态自动更新并保留历史事件。

- [x] T12 对账任务（元数据 vs 文件系统）
  - 验收标准：可定期发现漂移并生成修复建议；误报率可控（在测试矩阵中通过）。

- [x] T13 发布前测试矩阵与文档收口
  - 验收标准：通过“20 轮迁移+回滚”“步骤中断恢复”“外接盘离线告警”三大门槛；发布文档齐全（已知限制、回滚手册、FAQ）。

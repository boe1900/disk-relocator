# 已知限制（V1）

日期：2026-03-05

1. 平台范围仅覆盖 macOS。
2. 迁移对象以冻结画像为准，不支持任意应用自动识别。
3. `experimental` 画像仍需用户手动确认风险后执行。
4. 健康与对账监控依赖本地进程存活，应用未启动时不执行周期检测。
5. `safe-fix` 仅覆盖无损修复动作（如临时目录清理、元数据状态对齐），不覆盖复杂数据恢复。
6. 对账发现 `critical` 漂移时，仍以人工确认后执行回滚为主策略。
7. 外接盘权限与系统安全策略（如 Full Disk Access）仍可能导致不可自动修复错误。
